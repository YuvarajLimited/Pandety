import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jsmobiledev.yuvaraj',
  appName: 'Pandety',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
